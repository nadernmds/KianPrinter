@model IEnumerable<Shop.Models.Brand>

@{
    ViewBag.Title = "Index";
    
}

@*<h2>Index</h2>*@

<p>
    @Html.ActionLink("جدید", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.brandName)
        </th>
        <th>
            @Html.DisplayNameFor(model=>model.enBrandName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.brandName)
        </td>
        <td>
            @Html.DisplayFor(modelItem=>item.enBrandName)
        </td>
        <td>
            @Html.ActionLink("ویرایش", "Edit", new { id=item.brandID }) |
            @Html.ActionLink("مشاهده", "Details", new { id=item.brandID }) |
            @Html.ActionLink("حذف", "Delete", new { id=item.brandID })
        </td>
    </tr>
}

</table>
