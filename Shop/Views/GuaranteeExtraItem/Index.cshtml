@model IEnumerable<Shop.Models.GuaranteeExtraItem>

@{
    ViewBag.Title = " لوازم جانبی";
    Layout = "~/Views/Shared/_PupularLayout.cshtml";
}

@*<h2>Index</h2>*@

<p>
    @Html.ActionLink("جدید", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.itemName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.itemName)
        </td>
        <td>
            @Html.ActionLink("ویرایش", "Edit", new { id=item.guaranteeExtraItemID }) |
            @Html.ActionLink("مشاهده", "Details", new { id=item.guaranteeExtraItemID }) |
            @Html.ActionLink("حذف", "Delete", new { id=item.guaranteeExtraItemID })
        </td>
    </tr>
}

</table>
