@model IEnumerable<Shop.Models.GuaranteeServiceCompany>

@{
    ViewBag.Title = "شرکت های گارانتی";
    Layout = "~/Views/Shared/_PupularLayout.cshtml";
}

@*<h2>Index</h2>*@

<p>
    @Html.ActionLink("جدید", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.companyName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.companyName)
        </td>
        <td>
            @Html.ActionLink("ویرایش", "Edit", new { id=item.guaranteeServiceCompanyID }) |
            @Html.ActionLink("مشاهده", "Details", new { id=item.guaranteeServiceCompanyID }) |
            @Html.ActionLink("حذف", "Delete", new { id=item.guaranteeServiceCompanyID })
        </td>
    </tr>
}

</table>
