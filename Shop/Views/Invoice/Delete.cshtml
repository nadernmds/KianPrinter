@model Shop.Models.Invoice

@{
    ViewBag.Title = "Delete";
   
}

<h2>حذف</h2>

<h3>آیا مطمئن هستید که می خواهید حذف کنید؟</h3>
<div>
    @*<h4>Invoice</h4>*@
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.description)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.description)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.date)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.date)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.InvoiceType.name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.InvoiceType.name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.User.username)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.User.username)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="حذف" class="btn btn-default" /> |
             @if (Model.invoiceTypeID == 1)
    {
        @Html.ActionLink("بازگشت به لیست", "RemittanceList")

    }
    else if (Model.invoiceTypeID == 2)
    {
        @Html.ActionLink("بازگشت به لیست", "receiptList")

    }
    else if (Model.invoiceTypeID == 3)
    {
        @Html.ActionLink("بازگشت به لیست", "transferList")

    }
        </div>
    }
</div>
