@model IEnumerable<Shop.Models.GuaranteeState>

@{
    ViewBag.Title = "وضعیت ها";
    Layout = "~/Views/Shared/_PupularLayout.cshtml";
}

@*<h2>Index</h2>*@

<p>
    @Html.ActionLink("جدید", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.stateName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.stateName)
        </td>
        <td>
            @Html.ActionLink("ویرایش", "Edit", new { id=item.guaranteeStateID }) |
            @Html.ActionLink("مشاهده", "Details", new { id=item.guaranteeStateID }) |
            @Html.ActionLink("حذف", "Delete", new { id=item.guaranteeStateID })
        </td>
    </tr>
}

</table>
