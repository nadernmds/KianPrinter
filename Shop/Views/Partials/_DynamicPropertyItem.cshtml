@model Shop.Models.filter
<ul   style="list-style:none;padding:0px">
    @foreach (var item in Model.productAttribute)
    {
    <li>
        <div style="padding-top:5px;display:flex">
            <input data-brandID="@item.value" style="margin-top:8px" @*onclick="getbrands(@Model.brandID)"*@ type="checkbox" />
            <label style="color:#3b3b3b">@item.value</label>
            @*  <p style="float:left;left:5px;padding-top:2px;color:#a6a6a6;font-size:10px">First</p>*@
        </div>

    </li>
    }
</ul>
